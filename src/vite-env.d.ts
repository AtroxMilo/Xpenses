/// <reference types="vite/client" />

/** ISO timestamp of the build, injected in vite.config.ts. Shown in Settings
 * so it's obvious which deploy a given device is actually running. */
declare const __BUILD_ID__: string
